name: portfolio
description: A Portfolio to seek for what I am.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.7 #+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  get:
  url_launcher: ^5.5.2
  clipboard: ^0.1.2+8
  flare_flutter: ^2.0.6
  video_player:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/attendance/
    - assets/cactosol/
    - assets/commercial1/
    - assets/myself/